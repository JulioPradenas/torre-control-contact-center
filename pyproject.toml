[project]
name = "torre-control-contact-center"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "google-cloud-pubsub>=2.39.0",
]
[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]